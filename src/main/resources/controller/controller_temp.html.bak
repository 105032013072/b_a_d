<!DOCTYPE html>
<html>
<head>
<meta charset="utf-8">
<title>${description}</title>
<script src="https://cdn.bootcss.com/prettify/r298/prettify.min.js"></script>
<link href="https://cdn.bootcss.com/prettify/r298/prettify.min.css" rel="stylesheet">
<link href="../style.css" rel="stylesheet" />
</head>
<body onload="PR.prettyPrint()">
<div id="wmd-preview" class="wmd-preview">
<h1> ${description}:</h1>
<ul class="toc">
	<#list requestNodes as requestNode>
	 <li><a href="#${requestNode.methodName}">${requestNode.description}</a></li>
	</#list>
</ul>
<hr>
<h2 id="调用service">调用service</h2>
<!--<ul class="toc" >
	<#list autowiredMapperList as autowiredMapper>
 <li style="list-style-type:none;">	<a href="#${autowiredMapper.type}">» ${autowiredMapper.description}</a></li>
	</#list>
</ul>-->
	<#list autowiredServiceList as autowiredService>
 <p">	<a href="../service/${autowiredService.type}.html">» ${autowiredService.description}</a></p>
	</#list>

<hr>


<#list requestNodes as requestNode>
	<h2 id="${requestNode.methodName}">${requestNode.description}</h2>
	
  <pre class="prettyprint lang-json">
   method：<#list requestNode.method as mthod>${mthod},</#list>
   url: ${requestNode.url}
  </pre>
	<p><strong>参数列表</strong></p>
	<table>
   <tr>
    		<th>参数名</th>
    		<th>类型</th>
    		<th>必须</th>
    		<th>描述</th>
   </tr>
   <#list interfaceNode.paramNodes as paramNode>
   <tr>
         <td >${paramNode.name}</td>
         <td >${paramNode.type}</td>
         <td >${paramNode.required}</td>
         <td >${paramNode.description}</td>
   </tr>
   </#list>
  </table>
  <p>返回结果: <a href="../${requestNode.androidCodeUrl}">Android Code</a> | <a href="../${requestNode.iOSCodeUrl}">IOS Code</a></p>
  
	
	
	
	
	
	
	
	
  <p><strong>方法名:</strong> &nbsp&nbsp${interfaceNode.methodName}</p>
  <p><strong>参数列表:</strong></p>
  <table>
   <tr>
    		<th>参数名</th>
    		<th>类型</th>
    		<th>描述</th>
   </tr>
   <#list interfaceNode.paramNodes as paramNode>
   <tr>
         <td >${paramNode.name}</td>
         <td >${paramNode.type}</td>
         <td >${paramNode.description}</td>
   </tr>
   </#list>
  </table>
  
  <p><strong>异常列表:</strong></p>
  <table>
   <tr>
    		<th>异常类型</th>
    		<th>描述</th>
   </tr>
   <#list interfaceNode.throwsNode as throws>
   <tr>
         <td >${throws.type}</td>
         <td >${throws.description?default("")}</td>
   </tr>
   </#list>
  </table>
  <p><strong>返回类型: </strong>${interfaceNode.returnNode.type}&nbsp&nbsp${interfaceNode.returnNode.description}</p>
  <#if (interfaceNode.returnNode.jsonString)??>
  <pre class="prettyprint lang-json">${interfaceNode.returnNode.jsonString}</pre>
 </#if>
</#list>

<hr>
</div>
</body>
</html>